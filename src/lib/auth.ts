// src/lib/auth.ts
// NOTE: NextAuth was removed in favor of Clerk. This file remains as a backward
// compatibility stub in case other modules import `authOptions`. Prefer using
// Clerk server helpers (e.g., `auth()` from `@clerk/nextjs`) instead.

export const authOptions: any = undefined;

